"""skel-mesh-parser (Python port).

Python port of this repo's `js` branch (spine-skeleton-binary-js): a
minimal, dependency-free parser for the skin/attachment section of Spine
skeleton binary files (.skel), supporting versions 3.8.x and 4.2.x only.

Not yet implemented -- this package currently exists as an installable
skeleton so the consuming project (AtlasToolkit) can declare the dependency
ahead of the actual port landing. See the `js` branch for the reference
implementation this port is porting from, field-for-field, with no
behavioral changes.
"""

__version__ = "0.1.0"
